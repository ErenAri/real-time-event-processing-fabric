apiVersion: 1

datasources:
  - name: PulseStream Prometheus
    uid: pulsestream-prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: true
